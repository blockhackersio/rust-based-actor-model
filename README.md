# Writing your own Rust-Based Actor Model
This repo is a companion repo to the article https://vpunk.sh/blog/actor-model-in-rust/

## Setup
If you have Nix installed, you can grab [devenv](https://devenv.sh) with `nix profile install nixpkgs#devenv`.

## Running the tests
```bash
cargo test
```
